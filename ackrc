# Always sort the files
--sort−files

# Always color, even if piping to a another program
--color

# Use "less −r" as pager
# --pager
# less -r

--ignore-dir=build/
--ignore-dir=bin/
--ignore-dir=lib/
--ignore-dir=data/
--ignore-dir=import/
--ignore-dir=log/
--ignore-dir=external/
--ignore-dir=output/
--ignore-dir=doc/
--ignore-dir=third_party/

--type-set=json=.json
